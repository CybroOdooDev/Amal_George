<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.pharma.settings</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="99"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Pharmaceutical ERP" string="Pharmaceutical ERP" name="pharmaceutical_erp" groups="base.group_system">
                    <block title="Vendor Management" name="vendor_management_setting_container">
                        <setting string="Vendor Qualification" help="Require vendors to be qualified before procurement.">
                            <field name="vendor_qualification"/>
                        </setting>
                    </block>
                    <block title="Audit" name="audit_setting_container">
                        <setting string="Audit Trail" help="Enable audit trail to track changes in critical records.">
                            <field name="audit_trail"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="pharma_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'pharmaceutical_erp', 'bin_size': False}</field>
    </record>

</odoo>
