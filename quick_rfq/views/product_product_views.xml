<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_product_rfq" model="ir.actions.server">
        <field name="name">Create RFQ</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="group_ids" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        <field name="binding_model_id" ref="product.model_product_product"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
        if records:
            action = records.action_create_rfq()
        </field>
    </record>

    <!-- Action for Product Variants (product.product) -->
    <record id="product_product_action_purchased" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">list,form,activity</field>
        <field name="context">{"search_default_filter_to_purchase": 1, "purchase_product_template": 1, "show_lst_price": False, "show_standard_price": True}</field>
        <field name="search_view_id" ref="product.product_search_form_view"/>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            No product variant found. Let's create one!
          </p>
        </field>
    </record>

    <!-- Menuitem for Product Variants under Products -->
    <menuitem id="menu_purchase_product_variant"
              name="Product Variants"
              action="product_product_action_purchased"
              parent="purchase.menu_purchase_products"
              sequence="25"/>

</odoo>
